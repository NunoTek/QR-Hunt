# QR Hunt Configuration

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Set to true when behind a reverse proxy (Caddy, nginx, etc.)
# This enables proper handling of X-Forwarded-* headers
TRUST_PROXY=false

# Database
DATA_DIR=./data

# Security
ADMIN_CODE=admin123
COOKIE_SECRET=change-this-to-a-random-string-in-production

# Optional: API URL for internal calls (usually auto-detected)
# API_URL=http://localhost:3000
